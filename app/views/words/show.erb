<h1> <%= @word.content.titleize %></h1>

<h2> Here are all available definitions of  "<%= @word.content.downcase %>": </h2>
<ul>
<% @word.definitions.each do |definition| %>
    <li> <%= definition.content %></li>
    <li> read more at 🤓:
    <% if definition.read_more_url? %>
      <%=link_to definition.read_more_url, definition.read_more_url %></li>
      <br>
    <% end %>
    
    <% if definition.meme_url? %>
        <li> check this meme 😏: 
        <br>
        <%= link_to image_tag(definition.meme_url, :width => "250", :crop => :fill), definition.meme_url %></li>
    <% end %>

    <%= button_to " 🗑 ", @definition, :method => :delete %>
    <%= link_to " 📝 ", edit_definition_path(definition) %>
    <hr>
<% end %>
</ul>

<%= link_to " ✳️ Add a new definition", new_definition_path %>
<br><br><br>
<%= button_to " 🚨 say bye to '#{@word.content.downcase}'", @word, :method => :delete %>